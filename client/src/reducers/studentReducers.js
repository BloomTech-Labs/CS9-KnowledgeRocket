const defaultState = {
    // Initialize Students Redux State/Store Portion Here
};

export default (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}